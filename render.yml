services:
  - type: web
    name: spacematch-api
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn spacematch_api:app --host 0.0.0.0 --port 10000
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.13
    plan: free
